# 工作日志 · 2026-09-03 · 041：P7 阶段收尾（7.2 Logo/时区 + 7.3 WoL/端口测试/二维码）

> 记录人：ZCode ｜ 性质：功能开发（dev-plan 7.2 收尾 + 7.3 工具箱补全）｜ P7 阶段完成（5/5）

---

## 需求（用户原意）

完成 P7 剩余内容：7.2 基础设置页补 Logo/时区，7.3 工具箱补 WoL/Ping 端口测试/二维码。

## 完成情况

### 7.2 收尾

- **Logo**：设置键 `general.logo`（白名单+默认空）；常规设置面板加 Logo URL 输入；侧栏有 Logo 时显示图片（max-height 44px），否则显示站点名文字；
- **时区**：设置键 `general.timezone`（默认 `system`）；常规面板时区下拉（跟随系统 + 常用 IANA 时区）；首页时钟/日期按所选时区显示（utils/clock.ts 增加 timeZone 参数，`system` 不传时区走浏览器本地）。

### 7.3 工具箱补全（三工具）

- **WoL 网络唤醒（M10-1）**：后端 `services/tools.py`——`normalize_mac`（分隔符/大小写规范化）+ `build_magic_packet`（6×FF + MAC×16 = 102 字节，dev-plan 单测关卡明确要求）+ `send_wol`（UDP 广播 255.255.255.255:9，SO_BROADCAST）；`wol_targets` 表 + 目标列表 CRUD + 一键唤醒；
- **Ping/端口测试（M10-3）**：`check_tcp_port` 从服务端发起 TCP 连接，返回通断与延迟 ms；前端卡片输入 host/port 即测；
- **二维码（M10-5）**：qrcode 库纯前端生成（dataURL PNG 220px），支持下载。

前端工具箱页新增三卡（WoL 含目标列表管理/端口测试含延迟结果/二维码含下载）。

## 测试

- 后端 `test_p7_tools.py` 4 例：魔术包构造（102 字节/FF 前缀/MAC 重复/分隔符等价）与非法 MAC、端口测试（监听 up/关闭 down）、工具端点（未认证 401/非法 MAC 422/WoL 发送 102 字节/port-check up/目标 CRUD）——全量 **128 passed**、ruff 干净；
- 前端 48 passed、tsc/ESLint/build 干净；浏览器实测端口测试「连通 · 0ms」、WoL「魔术包已发送」、二维码渲染。

## 阶段状态

**P7 全部完成（5/5）**：7.1 设置框架 ✅ / 7.2 基础设置页 ✅（常规含 Logo/时区） / 7.3 工具箱 ✅（6 工具全） / 7.4 用户管理 ✅ / 7.5 可见性与访客 ✅。进度 **55 / 120**。

## 遗留 / 下一步

1. **P8 Docker 交付与 M1 集成验收**（compose 完整化/健康自检/M1 全量回归）为下一阶段；
2. P3 真实双环境验收仍待用户实测；
3. M01-11/10 等功能点均已在 7.4/7.5 提前交付。
