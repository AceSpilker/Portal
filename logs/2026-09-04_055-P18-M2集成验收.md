# 055 · P18 · M2 集成验收（五场景回归 + 性能 + 安全）

## 需求原意

**P18 · M2 集成验收**（2 步）：18.1 按《功能详述》M2 功能点清单全量回归；18.2 性能与安全检查（首页加载 <1s、接口压测冒烟、越权与注入扫描）。M2 验收五场景：应用离线告警 / 服务器指标阈值告警 / 端口断通告警 / AI 意图导航 / 定时通知。

## 验收结果

### 18.1 M2 全量回归 ✅

- **自动化回归基线**：后端 pytest **184 passed**（覆盖 P0~P17 全部 API 契约与权限）+ ruff 全绿；前端 vitest 48 / vue-tsc / eslint / build 全绿（含 PWA service worker 产物）；
- **五场景证据复核**（各阶段浏览器端到端 + 当前环境数据佐证）：
  1. 应用离线告警——P6 端到端（死端口磁贴红点+站内通知+恢复通知）；本次复核 dev 库通知中心仍存「Jellyfin 已下线」记录；
  2. 服务器指标阈值告警——P10 端到端（CPU>5% 规则约 2min 触发，铃铛+事件同步）；
  3. 端口断通告警——P11 端到端（9911 临时服务 up→kill→「断」+port_down 通知+事件流水）；
  4. AI 意图导航——P13 端到端（本地 mock OpenAI 兼容上游，「帮我打开下载器」返回 navigate 协议自动跳转）；
  5. 定时通知——P14 webhook token 触发→HTTP 动作→通知插值落站内全链路，cron 触发器重启恢复已验证；真实业务 cron 场景（如每日 qB 完成数推送）待用户配置后观察（不阻塞验收，机制已验证）。

### 18.2 性能与安全 ✅

- **性能**：
  - 首页（dev 模式，Vite HMR 真实资源）DOMContentLoaded **274ms**，导航到可交互墙钟 **315ms**（<1s 达标）；
  - 接口压测冒烟：`GET /api/apps` 200 次串行 **p50=1.5ms / p95=2.1ms / p99=3.2ms**；`/api/health` 平均 1.0ms；
- **安全冒烟（20 项检查全部符合契约）**：
  - 未认证访问 apps/users/audit-logs/health-report 均 **401**；
  - 普通 user 调管理接口（users/settings 写/audit/docker/backup/downloads/media/factory-reset/POST apps）全部 **403**；
  - 注入冒烟：keyword 带 `' OR 1=1--`、`) ;DROP TABLE apps;--` 均正常 200（SQLAlchemy 参数化），数据完好；非法 path id 422；
  - 说明：`GET /api/settings`（A 读 M 写，登录页需要站点名）与 `GET /api/tokens`（仅本人 Token）对普通用户 200 为契约设计，非越权。

## 遗留（移交 M3/实机）

- NAS 实机 48h 长稳、双网段自动切换、Bark/TG/SMTP 真机收包、真实 AI Key、qB/Jellyfin 实机联动——均为"实机环境"验收项，随部署执行；
- security.force_totp 键已入库，登录强阻断 UI 未做（记 M3 P21 观察项）。

**结论：M2 里程碑关闭（本机口径）。**
