# 工作日志 · 2026-09-02 · 028：Mac 开发环境搭建与项目运行

> 记录人：ZCode ｜ 性质：环境搭建（新开发机 macOS / Apple Silicon）

---

## 需求（用户原意）

1. 从 Gitee 拉取仓库 `https://gitee.com/acespilker/portal.git` 到本机项目目录；
2. 了解项目用途、logs 工作日志规范与项目约束；
3. 把项目在本机跑起来，之后在这台 Mac 上开发。

## 完成情况

### 1. 代码拉取

仓库已克隆到 `/Users/linzhongyang/ZCodeProject/Portal`，`main` 分支，最新提交 `f744fb3`。

### 2. 后端环境

- 本机 Python 3.12.6，新建 `backend/.venv`，安装 `requirements-dev.txt`；
- **修复一**：本机 pip 全局配置了清华镜像源，且官方 Python 未装 CA 证书，SSL 校验失败。处理：venv 内引导安装 `certifi`，并写入 `backend/.venv/pip.conf`（`cert = …/certifi/cacert.pem`），恢复镜像源正常校验（venv 自带配置，不污染全局）；
- **修复二**：SQLAlchemy 异步模式依赖 `greenlet`，但 SQLAlchemy 仅在部分平台自动安装（Windows/AMD64 会带，macOS arm64 不带），导致 61 个测试 ERROR。已在 `backend/requirements.txt` 显式补充 `greenlet>=3.0`；
- 后端测试 **63 passed**（与 027 日志记录一致），uvicorn 已启动在 `http://127.0.0.1:8000`（`--reload`）。

### 3. 前端环境

- 本机经 nvm 安装有 Node v16/v18/v23，采用 **v23.5.0**（npm 10.9.2，registry 已配 npmmirror）；
- `npm install` 285 包完成（ESLint 10 对 Node 23 有 EBADENGINE 警告，实际运行无影响）；
- Vitest **18 passed**（与 027 日志一致），Vite dev server 已启动在 `http://localhost:5173`。

### 4. 联调验证

- 直连后端 `GET /api/health` → `{"code":0,…,"initialized":false}`；
- 经前端 5173 代理 `/api/health` → 同样正常，`/ws`、`/icons` 代理配置已在 vite.config.ts 中生效；
- 前端首页 HTTP 200。**注意：本机是全新 SQLite 库（`initialized:false`），首次打开页面需走管理员初始化流程。**

## 关键决策

1. `greenlet` 直接补进 `requirements.txt` 而非只装进 venv——保证任何 arm64 Mac 新机_setup_一次通过（Windows 不受影响）；
2. pip 证书问题只在 venv 内解决（`.venv/pip.conf`），不改用户全局 pip 配置；
3. Node 选已有的 v23.5.0，不新装版本；未改 nvm 全局 default。

## 遗留 / 下一步

1. `requirements.txt` 的 greenlet 变更尚未提交，待确认后随下次任务入库；
2. 本机 Node 23.5.0 非 LTS，若后续 ESLint/Vite 升级出现兼容问题，可 `nvm install 22` 切 LTS；
3. 开发进度继续按 dev-plan 推进：下一阶段为 P3 网络环境与智能解析（另见 027 遗留：工具箱待补 WoL/Ping/二维码）。
